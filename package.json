{
  "name": "button-automater",
  "version": "1.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "save": "npm run build && npm run patch:version && git add . && git commit -m \"update\" && git push",
    "patch:version": "npm --no-git-tag-version version patch",
    "start": "node dist/index",
    "watch": "ts-node-dev src/index --watch",
    "build": "tsc -p ./tsconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "controller-helper": "git+https://github.com/ackerapple/usb-controller-repl.git",
    "open": "^8.2.1",
    "robotjs": "^0.6.0",
    "rxjs": "^7.1.0"
  },
  "devDependencies": {
    "ts-node": "^10.0.0",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.3.4"
  }
}
